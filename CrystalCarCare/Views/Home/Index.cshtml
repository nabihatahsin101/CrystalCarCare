<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crystal Car Care - Premium Automotive Services</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary: #003087;
            --secondary: #00205b;
            --accent: #00a8e8;
            --light: #f8f9fa;
            --dark: #1a252f;
            --success: #2ecc71;
            --neutral: #121212;
            --border-radius: 12px;
            --shadow: 0 10px 25px rgba(0,0,0,0.08);
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: var(--neutral);
            color: var(--dark);
            margin: 0;
            padding: 0;
        }

        /* Hero Carousel */
        .hero {
            width: 100%;
            height: 100vh;
            min-height: 600px;
            overflow: hidden;
          
        }

        .carousel-inner {
            width: 100%;
            height: 100%;
        }

        /* Overlay */
        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center;
            padding: 0 1rem;
            background: rgba(0,0,0,0); /* Slight overlay for better text contrast */
            color: #FFD700;
        }

       .hero-overlay h1 {
            color: #FFD700;
            font-weight: 900; /* Maximum boldness */
            font-size: clamp(2.5rem, 6vw, 4.5rem); /* Larger size for impact */
            text-shadow: 2px 2px 5px rgba(0,0,0,0.6); /* Stronger shadow */
            letter-spacing: 2px; /* Slight spacing for premium feel */
            text-transform: uppercase; /* Uppercase for emphasis */
            animation: glow 2s ease-in-out infinite alternate; /* Subtle glow animation */
        }

        @@keyframes glow {
            from {
                text-shadow: 2px 2px 5px rgba(0,0,0,0.6), 0 0 10px rgba(255,215,0,0.5);
            }
            to {
                text-shadow: 2px 2px 5px rgba(0,0,0,0.6), 0 0 20px rgba(255,215,0,0.8);
            }
        }

        .hero-overlay p {
            color: #FFD700;
            font-weight: 500; /* Slightly bolder for readability */
            font-size: clamp(1rem, 2.5vw, 1.5rem);
            text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
            max-width: 600px;
        }

        .btn-cta {
            background: #FFD700;
            color: #111;
            padding: 0.85rem 1.75rem; /* Slightly larger padding */
            border-radius: 30px; /* Slightly rounder */
            text-decoration: none;
            font-weight: 500;
            font-size: clamp(1.25rem, 3vw, 1.5rem); /* Larger text */
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }

            .btn-cta:hover {
                background: #FFA500;
                transform: scale(1.1);
                box-shadow: 0 12px 30px rgba(0,0,0,0.2); /* Enhanced hover shadow */
            }

            .btn-cta .arrow-circle {
                background: #fff;
                border-radius: 50%;
                width: 28px; /* Slightly larger for balance */
                height: 28px;
                display: flex;
                align-items: center;
                justify-content: center;
            }

            .btn-cta .bi-arrow-up-right {
                color: #111;
                font-size: 1.2rem; /* Slightly larger arrow */
            }


        /* Services */
        .services {
            padding: 3rem 0;
        }

        .section-title h2 {
            font-size: clamp(1.75rem, 4vw, 2.5rem);
            margin-bottom: 1rem;
            font-weight: 700;
            color: #FFD700;
        }

        .section-title p {
            color: #e0e0e0;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .service-card {
            background: #1a1a1a;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: 0.3s;
            display: flex;
            flex-direction: column;
        }

        .service-card:hover {
            transform: translateY(-8px);
        }

        .service-img {
            width: 100%;
            height: 220px;
            object-fit: cover;
        }

        .service-content {
            padding: 1.5rem;
            flex-grow: 1;
        }

        .service-card h3 {
            color: #FFD700;
        }

        .service-card p {
            color: #e0e0e0;
        }

        .btn-outline {
            border: 1px solid #FFD700;
            color: #FFD700;
            padding: 0.5rem 1rem;
            border-radius: var(--border-radius);
            text-decoration: none;
            transition: 0.3s;
            display: inline-block;
            margin-top: auto;
        }

        .btn-outline:hover {
            background: #FFD700;
            color: #111;
        }
        /* Features */
        .features {
            background: #111;
            padding: 4rem 0;
        }

        .features-grid {
            display: flex;
            flex-direction: row;
            flex-wrap: nowrap;
            justify-content: center;
            gap: 2rem;
            overflow-x: auto; /* Horizontal scrolling on small screens */
            padding-bottom: 1rem; /* Space for scrollbar */
        }

        .feature-item {
            background: #1a1a1a;
            border-radius: var(--border-radius);
            text-align: center;
            padding: 2.5rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: var(--shadow);
            color: #FFD700;
            flex: 0 0 250px; /* Fixed width for each item */
            min-width: 250px; /* Ensure consistent size */
        }

            .feature-item p {
                color: #e0e0e0;
                font-size: 1rem;
            }

        .feature-icon {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
        }

        .feature-item:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 30px rgba(0,0,0,0.2);
        }

        /* CTA Section */
        .cta {
            background: #111;
            color: #FFD700;
            text-align: center;
            padding: 3rem 1rem;
        }

        .cta p {
            color: #e0e0e0;
        }

        .cta .btn-light {
            background: #FFD700;
            color: #111;
            padding: 0.75rem 1.5rem;
            border-radius: var(--border-radius);
            font-weight: 500;
            transition: 0.3s;
            border: none;
        }

        .cta .btn-light:hover {
            background: #FFA500;
            transform: scale(1.05);
        }

        /* Responsive */
        @@media (max-width: 768px) {
            .services-grid, .features-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>

    <!-- Hero Section with Carousel -->
    <section class="hero">
        <div id="carouselExampleAutoplaying" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="@Url.Content("~/Content/img/carwash (2).jpg")" class="d-block w-100" alt="Car Wash">
                </div>
                <div class="carousel-item">
                    <img src="@Url.Content("~/Content/img/mechanic service.jpg")" class="d-block w-100" alt="Mechanic Service">
                </div>
                <div class="carousel-item">
                    <img src="@Url.Content("~/Content/img/detail.jpg")" class="d-block w-100" alt="Detailing Service">
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleAutoplaying" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleAutoplaying" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>

        <!-- Overlay -->
        <div class="hero-overlay">
            <h1>Crystal Car Care</h1>
            <p>Your car deserves the best. Experience luxury car detailing and services like never before.</p>
            <a href="/User/Booking" class="btn-cta" aria-label="Book an appointment now">
                Book Now
                <span class="arrow-circle"><i class="bi bi-arrow-up-right"></i></span>
            </a>
        </div>
    </section>

    <!-- Services Section -->
    <section class="services" id="services">
        <div class="container">
            <div class="section-title">
                <h2>Our Services</h2>
                <p>Comprehensive automotive services to keep your vehicle in pristine condition.</p>
            </div>
            <div class="services-grid">
                <div class="service-card">
                    <img src="https://images.unsplash.com/photo-1520340356584-f9917d1eea6f?q=80&w=1331&auto=format&fit=crop" alt="Car Wash" class="service-img">
                    <div class="service-content">
                        <h3>Car Wash</h3>
                        <p>Professional exterior and interior cleaning that leaves your vehicle sparkling clean.</p>
                        <a href="/services/carwash" class="btn-outline">Learn More</a>
                    </div>
                </div>
                <div class="service-card">
                    <img src="https://images.unsplash.com/photo-1618783129985-dd97dbe4ad99?q=80&w=687&auto=format&fit=crop" alt="Mechanic Services" class="service-img">
                    <div class="service-content">
                        <h3>Mechanic Services</h3>
                        <p>Expert maintenance and repairs by certified technicians using premium parts.</p>
                        <a href="/services/mechanic" class="btn-outline">Learn More</a>
                    </div>
                </div>
                <div class="service-card">
                    <img src="https://images.unsplash.com/photo-1471289549423-04adaecfa1f1?auto=format&fit=crop&w=600&q=80" alt="Detailing Services" class="service-img">
                    <div class="service-content">
                        <h3>Detailing Services</h3>
                        <p>Premium detailing and paint protection to enhance your vehicle's appearance.</p>
                        <a href="/services/additional" class="btn-outline">Learn More</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <!-- Features Section -->
    <section class="features" id="features">
        <div class="container">
            <div class="section-title">
                <h2>Why Choose Us</h2>
                <p>Discover what sets Crystal Car Care apart as the premier choice for automotive enthusiasts.</p>
            </div>
            <div class="features-grid">
                <div class="feature-item"><i class="fas fa-clock feature-icon"></i><h3>Quick Service</h3><p>Efficient, high-quality service that respects your time.</p></div>
                <div class="feature-item"><i class="fas fa-star feature-icon"></i><h3>Quality Guarantee</h3><p>Every service comes with our commitment to satisfaction.</p></div>
                <div class="feature-item"><i class="fas fa-tools feature-icon"></i><h3>Expert Technicians</h3><p>Certified professionals with extensive industry experience.</p></div>
                <div class="feature-item"><i class="fas fa-dollar-sign feature-icon"></i><h3>Competitive Pricing</h3><p>Premium services at prices that offer exceptional value.</p></div>
            </div>
        </div>
    </section>


    <!-- CTA Section -->
    <section class="cta" id="cta">
        <h2>Elevate Your Vehicle's Care Today</h2>
        <p>Schedule your appointment with Crystal Car Care and experience unparalleled automotive services.</p>
        <a href="/booking" class="btn-light">Schedule Appointment</a>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>